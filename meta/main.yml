---
galaxy_info:
  author: Andrea Pellegrini
  description: Ansible role to manage timemachine settings in macOS.
  license: MIT
  min_ansible_version: 2.4.0.0
  platforms:
    - name: Darwin
      versions:
        - all
  galaxy_tags:
    - system
    - osx
    - macos
    - config
    - timemachine
    - backup
dependencies:
  - { role: uschti.macos-defaults,
      macos_defaults: {
        defaults: []
      }
    }
